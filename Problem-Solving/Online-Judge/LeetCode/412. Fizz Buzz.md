# Fizz Buzz

[LeetCode](https://leetcode.com/problems/fizz-buzz/)

## Problem Description

Given an integer n, return a string array answer (1-indexed) where:

*   answer[i] == `"FizzBuzz"` -> if i is divisible by 3 and 5.
*   answer[i] == `"Fizz"` -> if i is divisible by 3.
*   answer[i] == `"Buzz"` -> if i is divisible by 5.
*   answer[i] == i (as a string) -> if none of the above conditions are true.
 
## Example & constraints
Example 1:
```
Input: n = 3
Output: ["1","2","Fizz"]
```
Example 2:
```
Input: n = 5
Output: ["1","2","Fizz","4","Buzz"]
```
Example 3:
```
Input: n = 15
Output: ["1","2","Fizz","4","Buzz","Fizz","7","8","Fizz","Buzz","11","Fizz","13","14","FizzBuzz"]
```

## Solution
```cpp
class Solution {
public:
    vector<string> fizzBuzz(int n) {
        vector<string> ans(n);
        for(int i=1;i<=n;i++){
            if(i%3==0 && i%5==0){ans[i-1]="FizzBuzz";}
            else if(i%3==0){ans[i-1]="Fizz";}
            else if(i%5==0){ans[i-1]="Buzz";}
            else {
                ans[i-1] = to_string(i);
            }
        }
        return ans;
    }
};
```
```cpp
//no to_string library
class Solution {
public:
    vector<string> fizzBuzz(int n) {
        vector<string> ans(n);
        for(int i=1;i<=n;i++){
            if(i%3==0 && i%5==0){ans[i-1]="FizzBuzz";}
            else if(i%3==0){ans[i-1]="Fizz";}
            else if(i%5==0){ans[i-1]="Buzz";}
            else {
                int temp = i;
                string temp1="";
                while(temp!=0){
                    temp1+=(temp % 10)+'0';
                    temp/=10;
                }
                reverse(temp1.begin(),temp1.end());
                ans[i-1]=temp1;
            }
        }
        return ans;
    }
};
```

## Explanation
*  這題就按照題目的敘述依序列下去，那其實就解出來了，就看最後面數字要不要手搓成字串，如果懂原理，可以直接用to_string。
  
## Time Complexity
### solution 1
*  $O(n)$
### solution 2
*  $O(nlogn)$
## Space Complexity
### solution 1
*  $O(n)$
### solution 2
*  $O(n)$





