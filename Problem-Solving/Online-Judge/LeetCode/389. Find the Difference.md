# Find the Difference

[LeetCode](https://leetcode.com/problems/find-the-difference/)

## Problem Description

You are given two strings s and t.

String t is generated by random shuffling string s and then add one more letter at a random position.

Return the letter that was added to t.

 
## Example & constraints
Example 1:
```
Input: s = "abcd", t = "abcde"
Output: "e"
```
Explanation: 'e' is the letter that was added.
Example 2:
```
Input: s = "", t = "y"
Output: "y"
```

Constraints:

*   0 <= s.length <= 1000
*   t.length == s.length + 1
*   s and t consist of lowercase English letters.

## Solution
```cpp
class Solution {
public:
    char findTheDifference(string s, string t) {
        int left=0,left1=0;
        vector<int> count(26,0);
        for(auto&c:s)count[c-'a']++;
        for(auto&c:t){
            count[c-'a']--;
            if(count[c-'a']<0)return c;
        }
        return ' ';
    
## Explanation
*  這題跟387. First Unique Character in a String還有383. Ransom Note幾乎一模一樣，只是說t比s多一個字母，所以就用vector去統計說每個字母出現幾次，然後再用for-loop跑一次t，如果是0那就是多出來的字母就回傳他，反之則回傳false。
  
  
## Time Complexity
*  $O(n)$
## Space Complexity
*  $O(1)$




